{
  "name": "ecole-des-loisirs-fondation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "directus:snapshot": "npx directus schema snapshot ./directus/schema/schema.yaml",
    "directus:apply": "npx directus schema apply ./directus/schema/schema.yaml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "directus": "^11.12.0"
  },
  "overrides": {
    "axios": "1.12.2"
  }
}
